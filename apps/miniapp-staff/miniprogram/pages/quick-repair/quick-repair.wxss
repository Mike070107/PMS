/* 卡片、缩略图、底部操作条、.kv 信息行都在 tokens.wxss / app.wxss 里统一定义，
   这里只放「按住说话」这一个本页独有的控件 */

/* 按住说话：现场戴手套、单手拿手机，做成卡片一整行宽。
   width / 左右 margin 必须 !important —— 微信 button 基础样式对这两项优先级比普通类高，
   不加的话按钮按文字宽度缩成一小块、被 auto margin 居中（app.wxss 的 .btn 同理，见那里的注释）。
   高度 96rpx：够老人戴手套按（≥88rpx 的下限），又不至于把「说一句话」这张卡撑成一整屏 */
.hold {
  width: 100% !important;
  margin-left: 0 !important;
  margin-right: 0 !important;
  box-sizing: border-box;
  height: 96rpx;
  margin-top: 8rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--radius-sm);
  background: var(--color-brand);
  color: #fff;
  font-size: var(--font-xl);
  font-weight: 600;
}
.hold::after { border: none; }
/* 录音中变色 + 轻微缩一下：戴手套时看不清小圆点，整块变色才看得出在录 */
.hold--on {
  background: var(--color-brand-dark);
  transform: scale(0.99);
}

/* 「按住说话」下面的用法说明。辅助说明用 26-28rpx（面向老人的下限），
   行高 1.6，别和上面的按钮挤在一起 */
.howto {
  margin-top: 16rpx;
  padding: 20rpx 24rpx;
  border-radius: var(--radius-sm);
  background: var(--color-bg);
}
.howto__line {
  font-size: var(--font-sm);
  color: var(--color-text-secondary);
  line-height: 1.6;
}
.howto__line + .howto__line { margin-top: 8rpx; }
/* 例句给出颜色，一眼看得出「这句是照着念的」 */
.howto__eg { color: var(--color-brand); }

/* 实时识别出来的半截话：让人知道系统在听、听成了什么 */
.partial {
  margin-top: 16rpx;
  padding: 16rpx 24rpx;
  border-radius: var(--radius-sm);
  background: var(--color-bg);
  color: var(--color-text-secondary);
  font-size: var(--font-md);
  line-height: 1.6;
}

/* 说完的整句落在这里，可以直接改 —— 语音识别不可能一次全对 */
.say { margin-top: 16rpx; min-height: 160rpx; }

/* 紧急那一行：整张卡是点击目标，戴手套也点得中 */
.urgency { display: flex; align-items: center; gap: 16rpx; }
.urgency--on {
  border-color: rgba(255, 77, 79, 0.5);
  background: rgba(255, 77, 79, 0.06);
}
.urgency__badge {
  flex-shrink: 0;
  padding: 6rpx 16rpx;
  border-radius: var(--radius-sm);
  background: var(--color-bg);
  color: var(--color-text-secondary);
  font-size: var(--font-sm);
  font-weight: 600;
}
.urgency--on .urgency__badge {
  background: rgba(255, 77, 79, 0.14);
  color: var(--color-danger);
}
.urgency__body { flex: 1; }
.urgency__text { font-size: var(--font-lg); font-weight: 500; line-height: 1.5; }
.urgency--on .urgency__text { color: var(--color-danger); }
.urgency__hint {
  margin-top: 6rpx;
  font-size: var(--font-sm);
  line-height: 1.6;
  color: var(--color-text-tertiary);
}
