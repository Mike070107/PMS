# docker-compose.prod.yml 的 env 文件
# 部署时拷贝为 docker/.env，按需替换强密码
# docker compose --env-file docker/.env -f docker/docker-compose.prod.yml up -d

POSTGRES_DB=pms_repair
POSTGRES_USER=pms
POSTGRES_PASSWORD=<change-me-strong>

REDIS_PASSWORD=<change-me-strong>

# 对象存储已迁移到腾讯云 COS（ap-shanghai bucket: property-repair-2026-1259497259），
# COS 凭据见 apps/api/.env
