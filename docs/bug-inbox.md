# Bug 收件箱

测试中发现的问题写这里，**不要打断正在执行的会话**（用 `/inbox <描述>`，或自己手写一行）。
`/inbox` 不带参数 = 按顺序把下面「待处理」做完。（别用 `/bug`：那是 Claude Code 内置命令，
会弹官方反馈框，记不进这个文件。）
处理完把 `[ ]` 改成 `[x]` 并补上修复的提交号；已修复满 20 条时把顶部旧的清掉。

格式：`- [ ] YYYY-MM-DD | 端 | 现象 | 复现路径`
端：owner(业主端) / staff(员工端) / web(后台) / api

## 待处理

<!-- 新条目追加在这一行下面 -->

- [ ] 2026-09-04 | staff | 小程序养护单没有做成 Web 端那张《房屋修理养护任务单》的实体单样式（现在是分区卡片）；要按 Web 的 sheet-geometry 版面在小程序里还原正反面 | 员工端 → 养护单 → 打开任一张
- [ ] 2026-09-04 | staff | 小程序页面说明文字太多，用户反馈太乱不知道点哪里，对老年人不友好。要求：默认只保留要填/要点的地方，说明文字收起、需要指导时再展开；**先出设计图给 Mike 看再动手** | 员工端各页（说明文字最多的：工单现场处理 19 处、材料与库存 13、快速报修 12、入库 12、工单池 11、我的 11、填表报修 9）

## 已修复

- [x] 2026-09-04 | staff+api | 办公室看不到本管理处的已完工单、维修工的「已完结」只有自己修的。根因：那一档走 scope=mine（只回 assignee=本人），办公室没「在手工单」权限时连这一档都没有。新增 scope=done：办公室（派单台/后台工单管理/企业管理员）看管理处范围内全部终态单；维修工看自己类别的（类型规则里列了他的类型 + 派给他 + 候选有他）；「在手工单」仍只认自己接的。终态清单按最近结束排 | 已修 bc3076f（API 已上线，员工端 1.0.20260904f）
- [x] 2026-09-04 | staff | 接完单角标还是旧数。根因：各格角标只在各自页面加载时设，没有统一入口。新增 utils/badges.ts + GET /work-orders/badge-counts，任意 tab 页 onShow、切回前台、接单后一次把工单池/派单台/在手/养护单/审批/我的一起对准，另有 60s 兜底轮询 | 已修 bc3076f
- [x] 2026-09-04 | staff | 从通知打开的工单详情点返回没反应、人卡住。根因：页面栈只有它一页，navigateBack 静默失败。utils/navigation.ts：退无可退回该去的 tab 页；Android 没有系统手势，页面根节点自己判「左边缘右滑」返回；三个自定义导航页（工单详情/快速报修/填表报修）一起接，返回箭头点击区放大到 96×88rpx | 已修 bc3076f
- [x] 2026-09-04 | staff | 签字画板随便写哪里都从左上角放射线。根因：旧版 canvas 每次 draw(true) 后路径清空，下一段 lineTo 没有 moveTo 就从 (0,0) 起笔。每一小段都从上一点起笔；点一下不拖也留点 | 已修 bc3076f

- [x] 2026-08-31 | staff | 工单池 data-first-ui 样式没生效 —— 2026-09-04 Mike 确认这条是很早以前提的、**已在别处处理**，不用再动。代码侧也复核过：pool.wxml 现在用 work-hero__* 一套类名，定义在 app.wxss（HEAD 里 6 处命中）。**别再按这条去改工单池样式**

- [x] 2026-09-04 | staff | 点底部「工单池」那一格，落在上次看的「已完结」而不是工单池。工单池是 tabBar 页不会重建，页内三档记在 page data 里；改成点 tab 时打一次性标记，页面据此回到工单池档（从工单详情返回仍保留原档）
- [x] 2026-09-04 | staff+api | 完工提交「点了没反应」，连点三次后两次报 `work order cannot be completed`。根因不是重复提交：提交前要同步弹微信订阅授权框，微信同时只显示一个弹窗，成功后的 showModal 被它吞掉，面板还开着、按钮又能按。改成成功后立刻收面板+轻提示，等授权框收场再弹结果；幂等令牌提交成功后不再清空（重复点原样返回上次结果）；顺带把 4 句会弹到用户脸上的英文报错改成中文并说清当前状态
- [x] 2026-09-04 | staff+owner | 错误反馈没记工单号，只能去翻 nginx 日志才知道是哪张单。反馈时优先取当前页面的工单号，取不到就从失败请求路径 `/work-orders/45/complete` 里抠 id，写进正文 `[工单 xxx]` 和 context；两个小程序同步改
- [x] 2026-09-04 | web | 工单详情抽屉没展示工单号（原来只在深蓝抽屉头上有一个白字的，用户没看到）。工单编号放进正文首屏那张卡的第一行，可复制

- [x] 2026-09-03 | web | 维修经验总结按管理处分组折叠（默认只展开当前那本所属的管理处），只有一个管理处时不套折叠面板。服务端本来就只下发数据范围内的管理处（allowedNotebooks → scopedOffices，跟随右上角管理处视角），所以只改了前端分组
- [x] 2026-09-03 | web | 图片上传改成可拖拽。**三处一起改**（一条反馈=一类入口）：共用的 MaterialPhotosUpload（经验笔记、材料 SKU 库、盘点）、入库单的实物照片、入库凭证附件。根因是只有 `type="drag"` 的 Upload 才绑 onDrop，普通 Upload 拖上去毫无反应，一律换成 Upload.Dragger（点击选文件照旧）。顺手把凭证列表里当图标用的 emoji 换成 SVG 图标
- [x] 2026-09-03 | web+api | 材料入库语音填表：新增 `POST /ai/material-receipt-parse`（新文件 material-receipt.ai.ts + material-receipt.controller.ts，走 inventory·edit 权限）。说一句 →识别出名称/规格/数量/单位/单价 → 回填入库明细；名称+规格都精确命中才预选 SKU，同名多规格一律给候选让人点，库里没有的给「建档并选用」（名称/规格/单位/参考单价预填，**类别必须人选**，因为类别决定材料编码前缀且发出去就锁死）。**只填表不提交**，一个字都不写库

- [x] 2026-08-31 | staff | 派单台的维修工列表不要显示「是否空闲」，改成显示「当前在手 X 单」 | 员工端 → 派单台 → 点某张单的「派单」，看维修工选择列表 | 已修 879da6b（0 单也写「在手 0 单」，绿色保留做「最闲的那个」的视觉提示）

- [x] 2026-08-31 | staff | 派单弹出的卡片向上滑不动，最下面的按钮被「工单池 / 派单台 / 在手工单」tabBar 挡住点不到（这类遮挡反复出现，要把所有弹层/半屏卡片整体排查一遍） | 员工端 → 派单台 → 点「派单」→ 试着滑到卡片底部。设计要点已写进全局 CLAUDE.md「弹层/半屏卡片」一节：弹层高度扣 tabBar + 安全区、内容区用 scroll-view 独立滚动、按钮条固定不参与滚动 | 已修 —— 但**第一版修错了，改了两轮**：① 9cfd792 把贴底浮层的 z-index 从 11 提到 201，以为胶囊 tabBar（z-index:100）就压不住了；用户在 1.0.20260831l 上复验，按钮**照样被挡**。② 根因其实是：自定义 tabBar 被微信渲染在页面之上的另一层，**不参与页面里的 z-index 比较**，这个前提一开始就是错的。正解是打开弹层期间把整条胶囊藏掉（utils/tabbar.ts 的 setTabBarHidden），tab 页里 pool 的派单面板、inventory 的详情/编辑面板共 6 个开关点全部接上。结论已回写 app.wxss 的层级账本和全局 CLAUDE.md 那条自查。

- [x] 2026-08-31 | staff+api | 语音报修抽取的结果没落进表单，四处：①报修位置语音里说了但没填入，转表单后显示「没有」②问题描述没把地址剥掉，「风华一期17号201，家里灯不亮」整句进了描述，应只留「家里灯不亮」；「猜你想输」的抽取同样不该带地址 ③联系人/电话被填成登录用户自己（叶双 / 15800603028），语音里已说「张先生 13800138000」，应以语音里的为准 ④用「枫桦景苑维修工」角色的账号提交报修报「权限不足」 | 员工端 → AI随手拍报修 → 按住说话，说「枫桦一期17号201家里灯不亮，联系人张先生13800138000」→ 看已认出的结果 → 改用完整表单。（识别文本把「枫桦」听成「风华」、电话多出一位「138000138000」，是否同因待查） | 已修 e12c617（四个现象两个根因：代报角色权限 403 + 转表单传了剥干净的描述；电话多一位给错号也一并修）

- [x] 2026-08-30 | web | 库存清单不跟随右上角的管理处视角：切了管理处，清单还是全部仓。右上角这个切换器应当是「本页所有数据的筛选范围」，切了就只看该管理处的小区仓 | 仓库与采购 → 库存清单 | 已修 c37695a
- [x] 2026-08-30 | web | 「仓库库存」标题后面要加「小区仓 / 总仓」的切换；总仓对谁可见由业务角色配置（不是所有管理处的人都该看到总公司总仓） | 仓库与采购 → 库存清单 | 已修 c37695a
- [x] 2026-08-30 | web | 仓库档案「所属管理处」显示成 `#5` 而不是管理处名称 | 仓库与采购 → 基础资料 → 仓库档案 | 已修 c37695a
- [x] 2026-08-30 | web | 仓库档案的「所属小区」栏位要删掉：一个管理处配一个管理处仓即可，而管理处下有多个小区，只挂一个小区会让人误以为别的小区不能用这个仓 | 仓库与采购 → 基础资料 → 仓库档案 | 已修 c37695a
- [x] 2026-08-30 | web+staff | 库位只能在仓库档案里增改，全流程没用起来：库存清单不显示库位；入库、调拨入库都不能选库位。要给仓库配默认入库库位，入库/调拨入库时带出默认值并允许改，库存清单加库位列 | 仓库与采购 → 库存清单 / 入库 / 调拨 | 已修 58f6781

