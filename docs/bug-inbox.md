# Bug 收件箱

测试中发现的问题写这里，**不要打断正在执行的会话**（用 `/inbox <描述>`，或自己手写一行）。
`/inbox` 不带参数 = 按顺序把下面「待处理」做完。（别用 `/bug`：那是 Claude Code 内置命令，
会弹官方反馈框，记不进这个文件。）
处理完把 `[ ]` 改成 `[x]` 并补上修复的提交号；已修复满 20 条时把顶部旧的清掉。

格式：`- [ ] YYYY-MM-DD | 端 | 现象 | 复现路径`
端：owner(业主端) / staff(员工端) / web(后台) / api

## 待处理

<!-- 新条目追加在这一行下面 -->

## 已修复

- [x] 2026-09-06 | api+deploy | 事故：上线 349c51f 时 InventoryController 新注入 NotificationsService 但 InventoryModule 没导入 NotificationsModule，Nest 起不来、线上 502 约 6 分钟（06:44–06:50）。build/单测/推送钩子都过（DI 只在运行时解析）。回滚时发现 `deploy/srv-deploy-api.sh` 不看参数、永远装 /tmp 最新包，把坏包又装了一遍；挪走坏包后回滚成功，随后 2a83577 修复上线。脚本已改为 `PKG=${1:-最新}`（web 同）并同步到服务器 | 部署
- [x] 2026-09-06 | api+staff | Mike 问：「我的 → 消息」是不是只显示跟我相关的（是：站内信按接收人一条条发，`notifications.receiver_id` 就是本人；「新工单待派」这类是按权限发给每个有那格权限的人）；并要求别处已处理过的消息自动变已读（工单池点开过的工单，消息里不该还是未读，其他同理）。改法：`NotificationsService.markReadByRef(user, ref)` 按 payload 里的单据 id 把本人未读一并标已读；挂在工单详情 GET、采购申请 查看/合并/提交/审批/驳回/改明细、采购单入库、调拨审批/驳回/收货、养护单详情上，只在操作成功后标 | 员工端 → 我的 → 消息
- [x] 2026-09-06 | api+staff | Mike：小程序端也要能点开采购申请看详情、编辑、补照片。新增 `GET /purchase-requests/:id` 和员工端页面 `pages/purchase-request`（全部照片可预览、名称/型号/备注/数量/单位可改、照片可补删最多 4 张、来源工单可点、申请原因与审批进度、办公室汇总阶段可直接「提交到下一环」）；审批页两种卡片都加了「详情 / 编辑 ›」入口。编辑层用 page-container 拦系统返回 | 员工端 → 审批 → 卡片「详情 / 编辑」
- [x] 2026-09-05 | web+api | Mike：「新建采购申请」里找不到合并申请一起提交审批的地方；并要求按小程序用料面板那套逻辑重做。原因：合并入口只在「采购申请 → 办公室汇总」页签里（默认「全部」页签没勾选框）。改法：①新弹窗 `components/PurchaseRequestCreateModal.tsx`——每行先搜材料库（编码/名称/型号，显示各仓库存），搜不到就地「申购新材料」只填名称，型号/样本照片/备注选填；下面列出所有待汇总申请可勾选一起并进来合成一张（服务端 POST /purchase-requests 新增 mergeRequestIds，以第一张为主单，新材料追加进去；只勾申请不填材料=纯合并）；②「全部」页签有待汇总的就出黄条「N 张申请在等办公室汇总 → 去汇总」；③保存后自动切到对应页签。Mike 补充要求（同日）：没有工单也能建、有工单可选填关联（搜单号/地址）；每行都能带照片和备注；提交前填「申请原因」（有新材料时必填，purchase_requests 新增 reason 列，Web 详情 / 员工端审批卡都显示）；两个出口「保存到办公室汇总」和「提交审批」（建单后直接推到下一环） | Web → 库存与采购 → 采购申请
- [x] 2026-09-05 | web | Mike：采购清单里点来源工单号直接跳到工单管理页，看完还得回来找位置；只是想顺便看一眼。改成只读的「看一眼」抽屉 `components/WorkOrderPeekDrawer.tsx`（地址 / 内容 / 类型 / 维修工 / 报修人 / 时间 / 缺料 / 现场照片，叠在申请详情之上，右上角才有「去工单管理处理」）；表单里所有来源工单链接都走它 | Web → 库存与采购 → 采购申请 / 采购单
- [x] 2026-09-05 | web | Mike：「新建采购申请」和「新建采购单」分不清。采购申请=要走审批的申请（办公室汇总→经理→采购→转采购单）；采购单=已经向供应商下的订单（记供应商、金额、到货入库），正常由审批通过的申请点「转采购单」生成。改法：「新建采购单」改名「直接下采购单（不走审批）」并加悬浮说明，只对公司级账号显示（受限账号服务端本来就拒）；采购单页签顶部一句话说明两者关系；页头主按钮换成「新建采购申请」 | Web → 库存与采购
- [x] 2026-09-05 | staff | 添加用料两个按钮（仓库材料 / 申购新材料）：要申购的人直接点申购，不会先查库存和 SKU，材料库里有的东西也让他填型号拍照。改成一个入口「添加材料」：先开材料库搜（有货直接领、库存 0 选中就进采购申请），列表底部才有「申购新材料「关键词」」；没选仓库也能直接申购。手填名称时在材料库里自动匹配，最多 3 条候选点一下直接换成 SKU 行，不用再填型号拍照。同一条（Mike）：样本照片不能必拍——缺料时手头没实物，标成「选填」，提示写明办公室在后台补图建档（提交本来就不校验照片）。员工端包 1.0.20260905i | 员工端 → 工单现场处理 → 记录用料

- [x] 2026-09-05 | web+staff+api | 办公室汇总：入口在 Web「库存与采购 → 采购申请 → 办公室汇总」和员工端「审批 → 待汇总」。新增「只合并不提交」（POST /purchase-requests/merge），合并后留在办公室汇总可继续编辑；编辑弹窗支持改照片、型号、备注、名称；Web 工具条拆成「合并成一张」和「提交」；员工端待汇总加「合并成一张」。申请详情材料明细表加横向滚动，摘要长名称截断 | 已修 766b97c
- [x] 2026-09-05 | staff | Mike 反馈右滑按层退「没变」。根因：iOS 的右滑是系统级手势，页面里自己判的边缘手势拦不住。改用微信 page-container 拦系统返回（iOS 右滑 / Android 物理键 / navigateBack），拦下后按层退：材料库 → 面板 → 页面；填表报修地址选择器逐级 → 页面。模拟器连发三次 navigateBack 验证 | 已修 9f20aca
- [x] 2026-09-05 | staff+web+api | 缺料→采购流程重设计（Mike 提出并点头）。A fe787a5：用料面板申购口径、就地申购新材料带型号/样本照片/备注；B 551000f：申请单明细带图/型号/备注、来源工单可点直达、小程序办公室待汇总勾选合并；C/D/E 9e5761d：审批链可配置（办公室/经理/采购各自开关+金额阈值，默认同旧链路）、转采购单缺档行说人话、员工端按采购单入库（选单→选总仓或小区仓→核实收）| 已修，最后一批 9e5761d
- [x] 2026-09-05 | staff | 边缘右滑按层退：手势结束先问页面 onEdgeBack，工单页先收材料库、再收面板、再退页面；填表报修地址选择器先退一级再关再退页；返回箭头同一逻辑；完工提交成功仍直接回列表。模拟器 callMethod 验证两次返回依次收层、页面栈不退 | 已修 0565115
- [x] 2026-09-05 | api+web+staff+owner | AI 费用：语音转文字全是微信插件（免费），只有 4 个接口走 DeepSeek（deepseek-v4-flash，按 token 计费）。做了四件事：①每次调用记 token/缓存命中/耗时进 ai_usage_logs，后台 AI 辅助页新增本月用量面板，填单价才估算费用 ②结果缓存 ai_result_cache，同一句话默认 30 天内复用，后台可调 ③提示词把每句都变的那行挪到最后、样例按 id 排稳，让 DeepSeek 前缀缓存命中整段 ④填表报修 lite 模式：规则先撞库、撞到楼栋/房号不调模型，防抖 400ms→1.2s | 已修 def309a
- [x] 2026-09-05 | web+staff+api | 角色管理没有工单「撤单」权限项。Mike 定：派单、撤回、作废分别独立授权。员工端矩阵新增「撤回工单」「作废工单」两格（勾中即生效），接口按新格鉴权，存量角色/模板按原「派单·操作」档自动回填，员工端工单页新增「作废工单」入口和面板；后台「工单管理·办理」仍一并包含。「撤单」(已撤单) 仍是业主撤自己报修的动作 | 已修 7f619bb
- [x] 2026-09-04 | staff | 语音报修页恢复带完整说明的版本（Mike 看过收起版后定的），「找谁联系」改「联系谁」；按住说话时不再插「正在听」面板，按钮位置不变只变色，提示语在按钮上下两行里切换，拍照入口和按钮拉开距离（模拟器截录音前后两张图核对过） | 已修 44b6111
- [x] 2026-09-04 | staff | 模拟器截图发现语音报修/工单现场处理/填表报修三页导航右上角「?」被微信胶囊压住点不到：新增 utils/custom-nav.ts 按胶囊实际位置排标题行，副标题单独一行 | 已修 44b6111
- [x] 2026-09-04 | staff | 小程序页面说明文字太多、太乱。按 2026-09-04 定稿的设计做了「指导层」：说明/示例句/步骤图默认收起，右上角「?」展开，一个开关全端通用（utils/guide.ts），首次使用默认打开；权限提示、报错、空态照常显示。改了 16 个页面：工单池、在手工单、工单现场处理（含四个面板）、语音报修、填表报修、材料与库存、入库、盘点、盘点计数、审批、消息、我的（两屏）、意见反馈、登录、维修经验 | 已修 5038942
- [x] 2026-09-04 | staff | 子页面一起改（同上，面板和自定义导航页都接了「?」） | 已修 5038942
- [x] 2026-09-04 | staff | 工单池只留一个报修入口「语音报修」（原「AI随手拍报修」），填表入口收进语音页的「不方便说话，改用填表报修」；在手工单本来就没有报修按钮 | 已修 5038942
- [x] 2026-09-04 | staff | 「记录用料 2」的 2 是已记录的用料数，不是步骤：按钮改成两行「记录用料 / 已记 N 项」；「添加维修进度」改「添加维修备注」（面板标题、语音按钮、提示语一起改） | 已修 5038942
- [x] 2026-09-04 | staff | 完工提交：完工照片放到第一步最上面，卡片改名「完工照片和说明」，第三张卡只剩本单用料 | 已修 5038942
- [x] 2026-09-04 | staff+web | 养护单字体：Web 端默认手写体「宅在家自动笔」只存在浏览器 localStorage、小程序拿不到，改为把该字体 GB2312 子集（1.8MB woff2 + woff 兜底）放到后台站点 /fonts，小程序 wx.loadFontFace 全局装入；nginx /fonts/ 加 CORS。6.2 元的来历：定额工料费合计 = (人工费 + 材料费) × 取费系数，系数默认 1.0341（后台「定额参数」可改），6.00 × 1.0341 = 6.20，不是预算定额里填的 | 已修 5038942
- [x] 2026-09-04 | staff+api | 维修经验列表：①每篇右侧一颗星，收藏的单独一块摊开在最上面，各本子默认收起点名字展开 ②范围本来就按人收敛（维修工只有自己工种，办公室整处），端上不再另筛 ③每个看得到的管理处附一本「本管理处公共」，同处的人都能看能写 ④搜索框：关键词在服务端只搜自己看得到的本子的标题和正文 | 已修 5038942
- [x] 2026-09-04 | staff+api | 办公室看不到本管理处的已完工单、维修工的「已完结」只有自己修的。根因：那一档走 scope=mine（只回 assignee=本人），办公室没「在手工单」权限时连这一档都没有。新增 scope=done：办公室（派单台/后台工单管理/企业管理员）看管理处范围内全部终态单；维修工看自己类别的（类型规则里列了他的类型 + 派给他 + 候选有他）；「在手工单」仍只认自己接的。终态清单按最近结束排 | 已修 bc3076f（API 已上线，员工端 1.0.20260904f）
- [x] 2026-09-04 | staff | 接完单角标还是旧数。根因：各格角标只在各自页面加载时设，没有统一入口。新增 utils/badges.ts + GET /work-orders/badge-counts，任意 tab 页 onShow、切回前台、接单后一次把工单池/派单台/在手/养护单/审批/我的一起对准，另有 60s 兜底轮询 | 已修 bc3076f
- [x] 2026-09-04 | staff | 从通知打开的工单详情点返回没反应、人卡住。根因：页面栈只有它一页，navigateBack 静默失败。utils/navigation.ts：退无可退回该去的 tab 页；Android 没有系统手势，页面根节点自己判「左边缘右滑」返回；三个自定义导航页（工单详情/快速报修/填表报修）一起接，返回箭头点击区放大到 96×88rpx | 已修 bc3076f
- [x] 2026-09-04 | staff+web | 小程序养护单做成 Web 端那张《房屋修理养护任务单》的实体单样式：纸面尺寸表搬到 packages/shared-types 两端共用；小程序按 mm × k 画正面（任务单 + 报修凭证存根）和背面（材料领耗记录 + 说明），本次要签的格黄底可点，签好落回纸上再提交；明细/材料超页自动分张 | 已修 74423dc（员工端 1.0.20260904g）
- [x] 2026-09-04 | staff | 签字画板随便写哪里都从左上角放射线。根因：旧版 canvas 每次 draw(true) 后路径清空，下一段 lineTo 没有 moveTo 就从 (0,0) 起笔。每一小段都从上一点起笔；点一下不拖也留点 | 已修 bc3076f

- [x] 2026-08-31 | staff | 工单池 data-first-ui 样式没生效 —— 2026-09-04 Mike 确认这条是很早以前提的、**已在别处处理**，不用再动。代码侧也复核过：pool.wxml 现在用 work-hero__* 一套类名，定义在 app.wxss（HEAD 里 6 处命中）。**别再按这条去改工单池样式**

- [x] 2026-09-04 | staff | 点底部「工单池」那一格，落在上次看的「已完结」而不是工单池。工单池是 tabBar 页不会重建，页内三档记在 page data 里；改成点 tab 时打一次性标记，页面据此回到工单池档（从工单详情返回仍保留原档）
- [x] 2026-09-04 | staff+api | 完工提交「点了没反应」，连点三次后两次报 `work order cannot be completed`。根因不是重复提交：提交前要同步弹微信订阅授权框，微信同时只显示一个弹窗，成功后的 showModal 被它吞掉，面板还开着、按钮又能按。改成成功后立刻收面板+轻提示，等授权框收场再弹结果；幂等令牌提交成功后不再清空（重复点原样返回上次结果）；顺带把 4 句会弹到用户脸上的英文报错改成中文并说清当前状态
- [x] 2026-09-04 | staff+owner | 错误反馈没记工单号，只能去翻 nginx 日志才知道是哪张单。反馈时优先取当前页面的工单号，取不到就从失败请求路径 `/work-orders/45/complete` 里抠 id，写进正文 `[工单 xxx]` 和 context；两个小程序同步改
- [x] 2026-09-04 | web | 工单详情抽屉没展示工单号（原来只在深蓝抽屉头上有一个白字的，用户没看到）。工单编号放进正文首屏那张卡的第一行，可复制

- [x] 2026-09-03 | web | 维修经验总结按管理处分组折叠（默认只展开当前那本所属的管理处），只有一个管理处时不套折叠面板。服务端本来就只下发数据范围内的管理处（allowedNotebooks → scopedOffices，跟随右上角管理处视角），所以只改了前端分组
- [x] 2026-09-03 | web | 图片上传改成可拖拽。**三处一起改**（一条反馈=一类入口）：共用的 MaterialPhotosUpload（经验笔记、材料 SKU 库、盘点）、入库单的实物照片、入库凭证附件。根因是只有 `type="drag"` 的 Upload 才绑 onDrop，普通 Upload 拖上去毫无反应，一律换成 Upload.Dragger（点击选文件照旧）。顺手把凭证列表里当图标用的 emoji 换成 SVG 图标
- [x] 2026-09-03 | web+api | 材料入库语音填表：新增 `POST /ai/material-receipt-parse`（新文件 material-receipt.ai.ts + material-receipt.controller.ts，走 inventory·edit 权限）。说一句 →识别出名称/规格/数量/单位/单价 → 回填入库明细；名称+规格都精确命中才预选 SKU，同名多规格一律给候选让人点，库里没有的给「建档并选用」（名称/规格/单位/参考单价预填，**类别必须人选**，因为类别决定材料编码前缀且发出去就锁死）。**只填表不提交**，一个字都不写库

- [x] 2026-08-31 | staff | 派单台的维修工列表不要显示「是否空闲」，改成显示「当前在手 X 单」 | 员工端 → 派单台 → 点某张单的「派单」，看维修工选择列表 | 已修 879da6b（0 单也写「在手 0 单」，绿色保留做「最闲的那个」的视觉提示）

- [x] 2026-08-31 | staff | 派单弹出的卡片向上滑不动，最下面的按钮被「工单池 / 派单台 / 在手工单」tabBar 挡住点不到（这类遮挡反复出现，要把所有弹层/半屏卡片整体排查一遍） | 员工端 → 派单台 → 点「派单」→ 试着滑到卡片底部。设计要点已写进全局 CLAUDE.md「弹层/半屏卡片」一节：弹层高度扣 tabBar + 安全区、内容区用 scroll-view 独立滚动、按钮条固定不参与滚动 | 已修 —— 但**第一版修错了，改了两轮**：① 9cfd792 把贴底浮层的 z-index 从 11 提到 201，以为胶囊 tabBar（z-index:100）就压不住了；用户在 1.0.20260831l 上复验，按钮**照样被挡**。② 根因其实是：自定义 tabBar 被微信渲染在页面之上的另一层，**不参与页面里的 z-index 比较**，这个前提一开始就是错的。正解是打开弹层期间把整条胶囊藏掉（utils/tabbar.ts 的 setTabBarHidden），tab 页里 pool 的派单面板、inventory 的详情/编辑面板共 6 个开关点全部接上。结论已回写 app.wxss 的层级账本和全局 CLAUDE.md 那条自查。

- [x] 2026-08-31 | staff+api | 语音报修抽取的结果没落进表单，四处：①报修位置语音里说了但没填入，转表单后显示「没有」②问题描述没把地址剥掉，「风华一期17号201，家里灯不亮」整句进了描述，应只留「家里灯不亮」；「猜你想输」的抽取同样不该带地址 ③联系人/电话被填成登录用户自己（叶双 / 15800603028），语音里已说「张先生 13800138000」，应以语音里的为准 ④用「枫桦景苑维修工」角色的账号提交报修报「权限不足」 | 员工端 → AI随手拍报修 → 按住说话，说「枫桦一期17号201家里灯不亮，联系人张先生13800138000」→ 看已认出的结果 → 改用完整表单。（识别文本把「枫桦」听成「风华」、电话多出一位「138000138000」，是否同因待查） | 已修 e12c617（四个现象两个根因：代报角色权限 403 + 转表单传了剥干净的描述；电话多一位给错号也一并修）

- [x] 2026-08-30 | web | 库存清单不跟随右上角的管理处视角：切了管理处，清单还是全部仓。右上角这个切换器应当是「本页所有数据的筛选范围」，切了就只看该管理处的小区仓 | 仓库与采购 → 库存清单 | 已修 c37695a
- [x] 2026-08-30 | web | 「仓库库存」标题后面要加「小区仓 / 总仓」的切换；总仓对谁可见由业务角色配置（不是所有管理处的人都该看到总公司总仓） | 仓库与采购 → 库存清单 | 已修 c37695a
- [x] 2026-08-30 | web | 仓库档案「所属管理处」显示成 `#5` 而不是管理处名称 | 仓库与采购 → 基础资料 → 仓库档案 | 已修 c37695a
- [x] 2026-08-30 | web | 仓库档案的「所属小区」栏位要删掉：一个管理处配一个管理处仓即可，而管理处下有多个小区，只挂一个小区会让人误以为别的小区不能用这个仓 | 仓库与采购 → 基础资料 → 仓库档案 | 已修 c37695a
- [x] 2026-08-30 | web+staff | 库位只能在仓库档案里增改，全流程没用起来：库存清单不显示库位；入库、调拨入库都不能选库位。要给仓库配默认入库库位，入库/调拨入库时带出默认值并允许改，库存清单加库位列 | 仓库与采购 → 库存清单 / 入库 / 调拨 | 已修 58f6781

