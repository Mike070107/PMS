# Bug 收件箱

测试中发现的问题写这里，**不要打断正在执行的会话**（用 `/bug <描述>`，或自己手写一行）。
处理完把 `[ ]` 改成 `[x]` 并补上修复的提交号；已修复满 20 条时把顶部旧的清掉。

格式：`- [ ] YYYY-MM-DD | 端 | 现象 | 复现路径`
端：owner(业主端) / staff(员工端) / web(后台) / api

## 待处理

<!-- 新条目追加在这一行下面 -->
- [ ] 2026-08-30 | web | 仓库档案「所属管理处」显示成 `#5` 而不是管理处名称 | 仓库与采购 → 基础资料 → 仓库档案
- [ ] 2026-08-30 | web | 仓库档案的「所属小区」栏位要删掉：一个管理处配一个管理处仓即可，而管理处下有多个小区，只挂一个小区会让人误以为别的小区不能用这个仓 | 仓库与采购 → 基础资料 → 仓库档案
- [ ] 2026-08-30 | web+staff | 库位只能在仓库档案里增改，全流程没用起来：库存清单不显示库位；入库、调拨入库都不能选库位。要给仓库配默认入库库位，入库/调拨入库时带出默认值并允许改，库存清单加库位列 | 仓库与采购 → 库存清单 / 入库 / 调拨

## 已修复

