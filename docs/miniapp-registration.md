# 微信小程序注册信息（草案）

> 适用于：开发方代客户（物业公司）申请。两个小程序分别申请 AppID，主体可以是同一家物业公司，也可以是开发方主体（视代运营协议而定）。

---

## 通用前置准备

| 项 | 说明 |
|---|---|
| 主体类型 | 企业（物业公司营业执照），非个体户 |
| 法人 / 管理员手机号 | 用于扫脸验证、找回 |
| 对公账户 | 用于打款 1 分钱验证主体 |
| 经营范围核对 | 营业执照需含「物业管理 / 物业服务 / 房地产经纪」等相关字样 |
| 服务器域名 | `api.<你的域名>.com`（HTTPS，已 ICP 备案）|
| 业务域名 | 若使用 web-view，需备案并下载校验文件 |

---

## 小程序 A · 业主端

**名称（首选）**：邻修
**名称（备选 1）**：邻修报修
**名称（备选 2）**：XX 物业（如「绿城物业」，主体名+物业）

> 命名规则提示：微信小程序名称全网唯一、不可与商标/已注册名称冲突；建议先用「微信公众平台 → 小程序注册 → 名称检测」预查。"邻修"两个字大概率被占，请以"邻修报修""<主体>邻修"作为备选。

### 简介（建议直接复制 80 字以内）

> 业主一站式社区报修小程序：扫码报修、实时查看工单进度、维修完成在线验收并评分，让小区维修更省心。

### 服务类目

- **主类目**：生活服务 → 物业服务
- **副类目**（视主体经营范围补充）：
  - 生活服务 → 家政

> 类目要求：物业服务类目需提交《物业服务企业资质证书》或营业执照中含物业相关经营范围。

### 隐私接口申请（《个人信息保护指引》中需声明）

| 接口 | 用途 |
|---|---|
| `wx.login` | 获取 openid / unionid 用于登录 |
| `wx.getPhoneNumber` | 业主入驻时填手机号免输入 |
| `wx.chooseLocation` / `wx.getLocation` | （可选）报修时辅助定位小区 |
| `wx.chooseImage` / `wx.chooseMedia` | 上传报修照片/视频 |
| `wx.scanCode` | 扫小区码 / 楼栋码进入报修 |
| 订阅消息模板 | 工单状态变更通知 |

### 需提前申请的订阅消息模板

1. **报修受理通知**：你的报修单已受理，正在派单中。
2. **维修工已接单**：维修工 {name} 已接单，预计 {time} 上门。
3. **维修完成待验收**：维修已完成，请前往小程序确认验收。
4. **工单已自动验收**：48 小时未确认，工单已自动关闭。

---

## 小程序 B · 物业端

**名称（首选）**：邻修工作台
**名称（备选 1）**：邻修物业
**名称（备选 2）**：XX 物业工作台

### 简介（建议直接复制）

> 物业团队协同工作台：工单派单与跟踪、维修执行与拍照留痕、材料领用与采购审批、统计看板，提升小区报修响应效率。

### 服务类目

- **主类目**：商业服务 → 企业管理
- **副类目**：
  - 工具 → 办公
  - 生活服务 → 物业服务（可选，与业主端保持一致便于消息互通）

> 物业端面向员工内部使用，建议**勾选"仅展示在管理员账号下，不对外公开"**（小程序 → 设置 → 基本设置 → 不在搜索结果展示）。

### 隐私接口申请

| 接口 | 用途 |
|---|---|
| `wx.login` | 员工登录 |
| `wx.getPhoneNumber` | 与后台预建账号关联 |
| `wx.scanCode` | 扫工单二维码、材料条码、签收单 |
| `wx.chooseMedia` / `wx.chooseImage` | 维修现场拍照、签到打卡 |
| `wx.getLocation` | 接单 / 到场 / 完工时核验定位 |
| `wx.makePhoneCall` | 一键拨打业主电话 |
| 订阅消息模板 | 新工单派发、超时预警、审批通知 |

### 需提前申请的订阅消息模板

1. **新工单派发**：你有一张新的工单 {orderNo}，请尽快处理。
2. **工单超时提醒**：工单 {orderNo} 已超 SLA，请关注。
3. **采购审批提醒**：有一笔采购申请待你审批。
4. **材料到货提醒**：采购单 {poNo} 已到货，请安排入库。

---

## 服务器/业务域名白名单

两个小程序均需在「开发 → 开发管理 → 开发设置 → 服务器域名」配置：

| 类别 | 域名 | 说明 |
|---|---|---|
| request 合法域名 | `https://prsznh.cn` | 所有业务接口 |
| uploadFile 合法域名 | `https://prsznh.cn` | 图片走 `POST /api/v1/upload`，由后端转存 COS，**不需要**配 COS 域名 |
| socket / downloadFile | （一期暂不用） | 图片用 `<image>` 展示，不受 downloadFile 白名单限制 |

**当前状态（2026-07-22 用微信接口读回）**：两个小程序均已配置 `request` 与 `uploadFile` = `https://prsznh.cn`。

配置入口：微信公众平台 → 开发管理 → 开发设置 → 服务器域名（每月限 50 次修改，两个小程序各配各的）。
自查命令（在服务器上跑，会用 .env 里的凭据取 token）：

```bash
curl -s -X POST "https://api.weixin.qq.com/wxa/getwxadevinfo?access_token=<TOKEN>&action=getbizdomain" -d '{}'
```

---

## 已启用的 AppID（2026-07-22 回填）

| 端 | 名称 | AppID | 代码位置 |
|---|---|---|---|
| 业主端 | 邻修 | `wx002fde4bfaa4c7d9` | `apps/miniapp-owner/project.config.json` |
| 员工端 | 邻修管理 | `wx8ef4de0e498064c4` | `apps/miniapp-staff/project.config.json` |

AppSecret **只存服务器** `/opt/pms-repair/apps/api/.env`，对应 `WX_OWNER_APPID/SECRET`、`WX_STAFF_APPID/SECRET`，
不写进仓库、不进日志、不下发前端。两个小程序的 `baseURL` 均指向 `https://prsznh.cn/api/v1`。

> 换 secret 只需改服务器 .env 后 `pm2 reload pms-api --update-env`，不用重新发版。

---

## 员工端登录与微信绑定流程

员工账号不自助注册，由管理员在后台「员工管理」预先建号（姓名 + 手机号 + 角色）。

1. 员工打开「邻修管理」→ 点「微信手机号一键登录」
   → `wx.getPhoneNumber` 拿到 code → 后端 `POST /auth/staff-login` 用 `getuserphonenumber` 解出手机号
   → 匹配到该手机号的在职员工账号 → 绑定本微信 openid → 发双 token。
2. 非维修工角色（办公室/经理/采购/管理员）也可用「账号密码登录」兜底，登录成功同样完成绑定。
3. 已绑定过的员工再次进入小程序只用 `wx.login` 的 code 静默登录，无需再次授权手机号。
4. 换手机/换微信/人员变动：管理员在后台员工列表点「解绑微信」，员工再登录一次即可重新绑定。

风险控制：
- 一个微信只能绑一个员工账号；一个员工账号只能绑一个微信，冲突时提示联系管理员解绑。
- 手机号命中多个员工账号（跨物业公司重复）时拒绝手机号登录，提示改用账号密码。
- 账号停用（`status=disabled`）后立即无法登录，已发 token 由 `/auth/me` 与各接口的用户校验拦截。

前置条件（微信侧）：员工端小程序需**企业主体已认证**才能使用「获取手机号」接口，
并在「小程序后台 → 设置 → 用户隐私保护指引」中声明 `wx.getPhoneNumber` 用途。
